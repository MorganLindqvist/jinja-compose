services:
  ubuntu0:
    command: tail -f /var/log/sysloog
    container_name: tail0
    image: ubuntu:latest
  ubuntu1:
    command: tail -f /var/log/sysloog
    container_name: tail1
    image: ubuntu:latest
  ubuntulog:
    command: tail -f /var/log/sysloog
    container_name: taillog
    image: ubuntu:latest
version: '3'
